[package]
name = "eye-care-core"
version = "0.3.0"
description = "Platform-neutral timer and window coordination for Eye Care Assistant"
authors = ["Builder Studio"]
edition = "2021"
rust-version = "1.77.2"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
